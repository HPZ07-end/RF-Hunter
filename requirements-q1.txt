# 核心计算：数组与向量运算。
# 待实现及验证完成后，再锁定实际使用的版本。
numpy

# 推荐：HiGHS线性规划，并与有理数消元交叉校核。
# 未安装时geometry.py可用标准库Fraction消元独立判断状态。
scipy

# 文档验收图：检测点、示向线、定位区域和覆盖圆。
matplotlib

# 可选测试工具；也可用标准库运行python -m unittest -v test_geometry。
pytest
